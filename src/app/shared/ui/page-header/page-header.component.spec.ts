import { PageHeaderComponent } from './page-header.component';

describe('PageHeaderComponent', () => {
  it('should create an instance', () => {
    expect(new PageHeaderComponent()).toBeTruthy();
  });
});
