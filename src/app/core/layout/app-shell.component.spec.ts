import { AppShellComponent } from './app-shell.component';

describe('AppShellComponent', () => {
  it('should create an instance', () => {
    expect(new AppShellComponent()).toBeTruthy();
  });
});
