import { ApiBaseUrlInterceptor } from './api-base-url.interceptor';

describe('ApiBaseUrlInterceptor', () => {
  it('should create an instance', () => {
    expect(new ApiBaseUrlInterceptor()).toBeTruthy();
  });
});
