import { CustomerDetailResolver } from './customer-detail.resolver';

describe('CustomerDetailResolver', () => {
  it('should create an instance', () => {
    expect(new CustomerDetailResolver()).toBeTruthy();
  });
});
